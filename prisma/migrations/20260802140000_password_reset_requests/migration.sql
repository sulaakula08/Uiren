-- Запрос на сброс пароля.
--
-- Почтовой рассылки у платформы нет, а школьные почты часто общие или
-- недоступны. Поэтому сброс идёт через администратора: человек оставляет
-- запрос, администратор видит его в панели и выдаёт временный пароль лично.

ALTER TABLE "User" ADD COLUMN "passwordResetAt" TIMESTAMP(3);
